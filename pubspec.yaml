name: waythru
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.1 <4.0.0"

dependencies:
  cloud_firestore: 
  equatable: ^2.0.5
  firebase_auth: null
  firebase_core: null
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_screenutil: ^5.9.0
  google_fonts: ^6.1.0
  hydrated_bloc: ^9.1.3
  iconsax: ^0.0.8
  path_provider: ^2.1.2

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
